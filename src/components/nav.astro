---
const isBlogPage = Astro.url.pathname.startsWith("/blog");
---

<header class="font-pixel">
  <nav id="site-nav" class="fixed z-990 top-0 sm:bottom-5 inset-x-0 sm:inset-auto sm:w-auto sm:left-1/2 sm:-translate-x-1/2 flex justify-between sm:justify-start items-center gap-3 sm:gap-3 p-3 sm:p-3 transition-colors duration-300 bg-transparent sm:bg-white/85 sm:backdrop-blur-xs sm:shadow-lg sm:shadow-black/6">
    <a href="/" id="nav-logo" class="max-sm:flex max-sm:items-center max-sm:justify-center max-sm:bg-white/85 max-sm:backdrop-blur-xs z-995 max-sm:shadow-lg max-sm:shadow-black/4 max-sm:p-3 sm:pl-2.5 transition-colors duration-300">
          <svg id="nav-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 234 78" class="h-4 w-auto fill-black transition-colors duration-300" data-astro-cid-l3rxwkef=""> <path d="m116.87,39.08C116.87,17.59,99.48.16,78,.09h0S0,.08,0,.08v78h78c21.48-.07,38.87-17.51,38.87-39Z" data-astro-cid-l3rxwkef=""></path> <rect x="117" width="39" height="78" data-astro-cid-l3rxwkef=""></rect> <polygon points="234 0 195 0 156 39 195 78 234 78 195 39 234 0" data-astro-cid-l3rxwkef=""></polygon> </svg>
    </a>
    <div id="nav-menu-wrapper" class="relative max-sm:bg-white/85 max-sm:backdrop-blur-xs max-sm:shadow-lg max-sm:shadow-black/4 transition-colors duration-300">
      <button id="nav-hamburger" type="button" aria-label="Toggle menu" aria-expanded="false" class="relative z-995 sm:hidden flex items-center justify-center size-10 text-black transition-colors duration-300">
        <svg class="hamburger-icon-open absolute size-6 fill-current"  viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
          <circle cx="20.5" cy="11.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="20.5" cy="20.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="17.5" cy="11.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="17.5" cy="20.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="14.5" cy="11.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="14.5" cy="20.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="11.5" cy="11.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="8.5" cy="11.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="5.5" cy="11.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="2.5" cy="11.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="11.5" cy="20.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="8.5" cy="20.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="5.5" cy="20.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="2.5" cy="20.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="11.5" cy="2.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="8.5" cy="2.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="5.5" cy="2.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="2.5" cy="2.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="14.5" cy="2.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="17.5" cy="2.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="20.5" cy="2.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
        </svg>
        <svg class="hamburger-icon-close absolute size-4 fill-current opacity-0" width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
          <circle cx="20.5" cy="20.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="17.5" cy="5.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="14.5" cy="8.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="11.5" cy="11.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="8.5" cy="8.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="8.5" cy="14.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="5.5" cy="17.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="17.5" cy="17.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="14.5" cy="14.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="2.5" cy="20.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="5.5" cy="5.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="2.5" cy="2.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
          <circle cx="20.5" cy="2.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"/>
        </svg>
      </button>
      <ul id="nav-menu" class="nav-menu-panel flex-col sm:flex-row sm:relative sm:mt-0 sm:bg-transparent sm:shadow-none sm:p-0 max-sm:bg-white max-sm:shadow-2xl max-sm:shadow-black/20 transition-colors duration-300">
        <li>
          <a id="home" href="/#hero-section" class:list={["py-1 px-3 text-nowrap flex gap-1 items-center max-sm:text-xl max-sm:py-3", { "font-bold": !isBlogPage }]}><kbd class="font-pixel hidden md:inline-block">[H]</kbd> Home</a>
        </li>
        <li>
          <a id="work" href="/#work-section" class="py-1 px-3 text-nowrap flex gap-1 items-center max-sm:text-xl max-sm:py-3"><kbd class="font-pixel hidden md:inline-block">[W]</kbd> Work</a>
        </li>
        <li>
          <a id="blog" href="/blog/" class:list={["py-1 px-3 text-nowrap flex gap-1 items-center max-sm:text-xl max-sm:py-3", { "font-bold": isBlogPage }]}><kbd class="hidden md:inline-block">[B]</kbd> Blog</a>
        </li>
        <li>
          <a id="contact" href="#contact-section" class="py-1 px-3 text-nowrap flex gap-1 items-center max-sm:text-xl max-sm:py-3"><kbd class="hidden md:inline-block">[C]</kbd> Contact</a>
        </li>
      </ul>
    </div>
  </nav>
  <div id="site-links" class="fixed z-995 bottom-3 left-3 sm:bottom-auto top-auto sm:top-2.5 sm:left-2.5 flex gap-2.5">
    <a href="#" class="site-link bg-white/85 items-center backdrop-blur-xs p-2 pr-2.5 sm:p-1 sm:pr-1.5 flex gap-2.5 transition-colors duration-300">
      <svg class="size-6 sm:size-4 fill-current" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
        <path
          d="M12 .297c-6.63 0-12 5.373-12 12 0 5.303 3.438 9.8 8.205 11.385.6.113.82-.258.82-.577 0-.285-.01-1.04-.015-2.04-3.338.724-4.042-1.61-4.042-1.61-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.084-.729.084-.729 1.205.084 1.838 1.237 1.838 1.237 1.07 1.835 2.809 1.305 3.495.998.108-.776.417-1.305.76-1.605-2.665-.3-5.466-1.332-5.466-5.93 0-1.31.465-2.38 1.235-3.22-.135-.303-.54-1.523.105-3.176 0 0 1.005-.322 3.3 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.292-1.552 3.297-1.23 3.297-1.23.645 1.653.24 2.873.12 3.176.765.84 1.23 1.91 1.23 3.22 0 4.61-2.805 5.625-5.475 5.92.42.36.81 1.096.81 2.22 0 1.606-.015 2.896-.015 3.286 0 .315.21.69.825.57C20.565 22.092 24 17.592 24 12.297c0-6.627-5.373-12-12-12"
        ></path>
      </svg>
      <p class="text-xs">Github</p>
    </a>
    <a href="#" class="site-link bg-white/85 items-center backdrop-blur-xs p-2 pr-2.5 sm:p-1 sm:pr-1.5 flex gap-2.5 transition-colors duration-300">
      <svg class="size-6 sm:size-4" viewBox="0 0 22 22" fill="none" xmlns="http://www.w3.org/2000/svg">
        <circle cx="12" cy="7.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"></circle>
        <circle cx="18" cy="7.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"></circle>
        <circle cx="15" cy="19.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"></circle>
        <circle cx="12" cy="19.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"></circle>
        <circle cx="9" cy="19.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"></circle>
        <circle cx="6" cy="19.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"></circle>
        <circle cx="3" cy="16.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"></circle>
        <circle cx="3" cy="13.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"></circle>
        <circle cx="3" cy="10.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"></circle>
        <circle cx="3" cy="7.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"></circle>
        <circle cx="3" cy="4.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"></circle>
        <circle cx="6" cy="1.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"></circle>
        <circle cx="9" cy="1.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"></circle>
        <circle cx="12" cy="1.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"></circle>
        <circle cx="12" cy="4.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"></circle>
        <circle cx="15" cy="4.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"></circle>
        <circle cx="15" cy="7.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"></circle>
        <circle cx="18" cy="10.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"></circle>
        <circle cx="18" cy="13.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"></circle>
        <circle cx="18" cy="16.5" r="1.125" fill="currentColor" stroke="currentColor" stroke-width="0.25"></circle>
      </svg>

      <p class="text-xs">CV</p>
    </a>
  </div>
</header>

<style>
  .hamburger-icon-open,
  .hamburger-icon-close {
    transition: opacity 300ms ease-in-out;
  }

  #nav-hamburger.nav-hamburger-open .hamburger-icon-open {
    opacity: 0;
  }
  #nav-hamburger.nav-hamburger-open .hamburger-icon-close {
    opacity: 1;
  }
  #nav-hamburger:not(.nav-hamburger-open) .hamburger-icon-close {
    opacity: 0;
  }

  .nav-menu-panel {
    display: flex;
  }

  @media (max-width: 639.98px) {
    .nav-menu-panel {
      position: fixed;
      top: -0.75rem;
      right: -0.75rem;
      bottom: -0.75rem;
      height: calc(100dvh + 1.5rem);
      width: 78vw;
      max-width: 22rem;
      flex-direction: column;
      justify-content: center;
      align-items: flex-start;
      gap: 1.5rem;
      padding: 2rem;
      transform: translateX(100%);
      transition: transform 320ms ease-in-out;
      z-index: 990;
    }

    .nav-menu-panel.nav-menu-open {
      transform: translateX(0);
    }
  }
</style>

<script>
  const keyMap: Record<string, string> = {
    h: "home",
    w: "work",
    b: "blog",
    c: "contact",
  };
  const navLinks = document.querySelectorAll<HTMLAnchorElement>("#site-nav ul a");

  function setSelected(id: string) {
    navLinks.forEach((link) => {
      if (link.id === id) {
        link.classList.add("font-bold");
      } else {
        link.classList.remove("font-bold");
      }
    });
  }

  navLinks.forEach((link) => {
    link.addEventListener("click", () => {
      setSelected(link.id);
      closeMenu();
    });
  });

  const hamburger = document.getElementById("nav-hamburger");
  const menu = document.getElementById("nav-menu");

  function closeMenu() {
    menu?.classList.remove("nav-menu-open");
    hamburger?.setAttribute("aria-expanded", "false");
    hamburger?.classList.remove("nav-hamburger-open");
  }

  function openMenu() {
    menu?.classList.add("nav-menu-open");
    hamburger?.setAttribute("aria-expanded", "true");
    hamburger?.classList.add("nav-hamburger-open");
  }

  hamburger?.addEventListener("click", () => {
    const isOpen = hamburger.getAttribute("aria-expanded") === "true";
    if (isOpen) {
      closeMenu();
    } else {
      openMenu();
    }
  });

  document.addEventListener("click", (e) => {
    const target = e.target as HTMLElement;
    if (!target.closest("#nav-menu-wrapper")) {
      closeMenu();
    }
  });

  document.addEventListener("keydown", (e) => {
    if (e.metaKey || e.ctrlKey || e.altKey) return;

    const target = e.target as HTMLElement;
    if (target.closest("input, textarea, select, [contenteditable='true']")) return;

    const id = keyMap[e.key.toLowerCase()];
    if (!id) return;

    const link = document.getElementById(id);
    if (link instanceof HTMLAnchorElement) {
      e.preventDefault();
      setSelected(id);
      link.click();
    }
  });
</script>

<script>
  import gsap from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";

  gsap.registerPlugin(ScrollTrigger);

  const nav = document.getElementById("site-nav");
  const navIcon = document.getElementById("nav-icon");
  const navLogo = document.getElementById("nav-logo");
  const navMenuWrapper = document.getElementById("nav-menu-wrapper");
  const navMenu = document.getElementById("nav-menu");
  const hamburger = document.getElementById("nav-hamburger");
  const siteLinks = document.querySelectorAll<HTMLAnchorElement>(".site-link");

  if (nav && navIcon) {
    const LIGHT = {
      navBg: "sm:bg-white/85",
      squareBg: "max-sm:bg-white/85",
      menuBg: "max-sm:bg-white",
      navText: "text-black",
      iconFill: "fill-black",
      linkBg: "bg-white/85",
    };
    const DARK = {
      navBg: "sm:bg-black/85",
      squareBg: "max-sm:bg-black/85",
      menuBg: "max-sm:bg-black",
      navText: "text-white",
      iconFill: "fill-white",
      linkBg: "bg-black/85",
    };

    const setTheme = (theme: "light" | "dark") => {
      const from = theme === "dark" ? LIGHT : DARK;
      const to = theme === "dark" ? DARK : LIGHT;

      nav.classList.remove(from.navText);
      nav.classList.add(to.navText);

      nav.classList.remove(from.navBg);
      nav.classList.add(to.navBg);

      [navLogo, navMenuWrapper].forEach((el) => {
        el?.classList.remove(from.squareBg);
        el?.classList.add(to.squareBg);
      });

      navMenu?.classList.remove(from.menuBg);
      navMenu?.classList.add(to.menuBg);

      navIcon.classList.remove(from.iconFill);
      navIcon.classList.add(to.iconFill);

      hamburger?.classList.remove(from.navText);
      hamburger?.classList.add(to.navText);

      siteLinks.forEach((link) => {
        link.classList.remove(from.linkBg, from.navText);
        link.classList.add(to.linkBg, to.navText);
      });
    };

    // The nav is fixed to the viewport, so its center is a stable screen-space
    // line: whichever section covers that line at any scroll position decides
    // the theme.
    const getNavCenter = () => {
      const rect = nav.getBoundingClientRect();
      return rect.top + rect.height / 2;
    };

    const navLinks = document.querySelectorAll<HTMLAnchorElement>("#site-nav ul a");

    const setSelected = (id: string) => {
      navLinks.forEach((link) => {
        link.classList.toggle("font-bold", link.id === id);
      });
    };

    // Some nav links (like Blog) point to a dedicated page rather than an
    // in-page hash, but still have a corresponding section on the homepage
    // that should highlight them while scrolled into view.
    const sectionOverrides: Record<string, string> = {
      blog: "#blog-section",
    };

    navLinks.forEach((link) => {
      const hash = sectionOverrides[link.id] ?? new URL(link.href, location.href).hash;
      const section = hash ? document.querySelector<HTMLElement>(hash) : null;
      if (!section) return;

      ScrollTrigger.create({
        trigger: section,
        start: () => `top ${getNavCenter()}px`,
        end: () => `bottom ${getNavCenter()}px`,
        onEnter: () => setSelected(link.id),
        onEnterBack: () => setSelected(link.id),
        invalidateOnRefresh: true,
      });
    });

    const themedSections = document.querySelectorAll<HTMLElement>("[data-nav-theme]");

    themedSections.forEach((section) => {
      const theme = section.dataset.navTheme === "dark" ? "dark" : "light";

      ScrollTrigger.create({
        trigger: section,
        start: () => `top ${getNavCenter()}px`,
        end: () => `bottom ${getNavCenter()}px`,
        onEnter: () => setTheme(theme),
        onEnterBack: () => setTheme(theme),
        invalidateOnRefresh: true,
      });
    });
  }
</script>
